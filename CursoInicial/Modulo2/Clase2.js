//Funciones

//Una función es un bloque de código que nos permite agrupar una funcionalidad
//para utilizar donde y cuando querramos.
//Normalmente realiza una tarea específica, y retorna un valor.

//Estructura básica.

function sumar (a+b){ //La función empieza con la palabra reservada 'function', seguido del nombre de la variable 'sumar', y (los valores con los que operamos), luego {}
	return a+b; // Dentro de las llaves va la operación que queremos realizar.
} //con esta } indicamos que finaliza la función.

//Esta función devuelve el resultado de a+b.

console.log(sumar(5,2)) //Llamando a la función sumar, pasándole los valores 5 y 2, le pedimos que nos devuelva el resultado, en este caso 7


